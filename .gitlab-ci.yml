stages:
  - test

# Job de test simple
test_job:
  stage: test
  before_script:
    - pwsh -Command "& 'C:/path/to/python.exe' --version"
    - pwsh -Command "pip --version"
    - pwsh -Command "pip install selenium webdriver_manager"
  script:
    - echo "c'est good"
  only:
    - dev
    - main
  tags:
    - my-shell-runner
